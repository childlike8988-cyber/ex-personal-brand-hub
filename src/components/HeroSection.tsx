import { Profile } from "@/types/profile";

export function HeroSection({ profile }: { profile: Profile }) {
  const generic = profile.type === "generic";
  return <section className="hero container" id="top"><div className="hero-copy"><p className="eyebrow">{profile.eyebrow}</p>{generic && <span className="demo-badge">通用行業展示 · Food & Lifestyle Demo</span>}<h1>{profile.hero.title.map((line) => <span key={line}>{line}</span>)}</h1><p className="hero-subtitle">{profile.hero.subtitle}</p><p className="hero-description">{profile.hero.description}</p><div className="actions"><a className="button button-gold" href="#contact">{profile.hero.primaryCta}</a><a className="button button-outline" href="#showcase">{profile.hero.secondaryCta}</a></div></div><div className="device-stage" aria-label={`${profile.label}網站預覽示意`}><div className="laptop"><div className="browser-bar"><i /><i /><i /><span>E.X <small>{generic ? "BRAND" : "REALTY"}</small></span><b>首頁　精選　關於</b></div><div className="screen"><div className="mock-copy"><small>{generic ? "THE TABLE" : "ER NEST REALTY"}</small><strong>{profile.hero.visualTitle}<br />{profile.hero.visualBody}</strong><p>{generic ? "一場值得被記住的味覺體驗" : "從看屋到交屋，陪伴每個重要選擇"}</p><em>{generic ? "查看菜單" : "查看物件"}</em></div><div className="screen-photo"><div className="mock-price">{generic ? "NT$ 1,680" : "1,680 萬"}</div></div></div><div className="mock-card-row"><span /><span /><span /></div></div><div className="phone"><div className="phone-notch" /><div className="phone-screen"><small>E.X</small><b>{profile.hero.visualTitle}</b><span>{profile.hero.visualBody}</span><div /><strong>{generic ? "NT$ 980" : "2,680 萬"}</strong></div></div></div></section>;
}
